"""
    This is to convert id and obtain the files from fastq so that it can be used to 
    convert into fasta and qual files later on.
"""


inFile = sys.argv[0] 
idList = [] 
with open(inFile, 'r') as rf: 
    for line in rf: 
        idList.append(line.strip("\n")

while True: 
    reads = sys.stdin.read()
